# Firmspace AI — план реализации

## Аудит текущего состояния

- GitHub-репозиторий `RustDzhur/ai-agent` содержит статический немецкоязычный лендинг (`frontend/public`) и отдельный Caddy-фрагмент для сайта.
- В репозитории нет API, frontend-приложения workspace, схемы БД, миграций, тестов или Docker-конфигурации приложения.
- На сервере Ubuntu уже работают Docker, Caddy, PostgreSQL 18.6, Redis 8 и MinIO. Все три хранилища проходят health check.
- База `firmspace` создана отдельно и пока не содержит таблиц в схеме `public`. Существующие серверные `.env` содержат секреты; их значения не читались и в Git не попадут.
- Публичный сайт сейчас обслуживается Caddy из `/opt/projects/firmspace/frontend/public`; адреса с `www` и без него покрыты одним Caddy-блоком.
- Локально доступен Node.js 24, но Docker установлен только на сервере.

## Принципы

- Приложение работает на `/app`, API — на `/api/v1`; публичный лендинг остаётся на `/`.
- Интерфейс workspace сначала делается на немецком языке, тексты отделяются от компонентов для последующего добавления English, Russian и Ukrainian.
- Метрики, агенты, задачи и интеграции показываются только после появления реальных данных и работающего backend.
- Каждая операция с данными организации требует серверной проверки членства и роли. Tenant-owned таблицы получают `tenant_id` и защиту PostgreSQL RLS.
- Агентам запрещён прямой доступ к PostgreSQL, Redis, MinIO и файловой системе; будущие действия проходят через ограниченный tool gateway.
- Секреты остаются только в серверном файле окружения с ограниченными правами. Платёжные, финансовые и внешние действия требуют аудита и человеческого подтверждения.

## Этапы

| Этап | Содержание | Состояние |
|---|---|---|
| 1. Основа | Структура приложения, конфигурация, PostgreSQL migrations, health/ready, request IDs, безопасные заголовки, контейнеризация и deployment | Готово локально; серверный rollout в работе |
| 2. Пользователи и tenants | Регистрация, вход/выход, серверные сессии, организации, роли/членство, переключение организации, audit log, RLS и tenant isolation | Готово локально; серверная проверка в работе |
| 3. Agent runtime | Registry версий шаблонов, безопасная очередь запусков, инструменты с allowlist, трассировка, остановка и повторный запуск | Запланирован |
| 4. Workspace и marketplace | Реальный каталог, установка агента, permissions, интеграции, страницы агента и его выполнения | Запланирован |
| 5. Workflow engine | Схема workflow, узлы/переходы, валидация, исполнение, одобрения человеком, идемпотентность | Запланирован |
| 6. Интеграции | OAuth и соединения с провайдерами, хранение зашифрованных секретов, health и отзыв доступа | Запланирован |
| 7. Документы и знания | Валидация и изоляция файлов, MinIO, document lifecycle, OCR, knowledge bases и безопасный поиск | Запланирован |
| 8. Бизнес-агенты | Email, invoice/OCR, CRM, lead qualification, sales, document, research, support, financial reports, workflow | Запланирован |
| 9. Аналитика и стоимость | Только данные реальных запусков, usage records, model policy и лимиты | Запланирован |
| 10. Billing и platform admin | Абстракция billing provider, планы, подписки, feature flags и администрирование каталога | Запланирован |
| 11. API и расширения | OpenAPI, webhooks, API keys, SDK, MCP и developer center | Запланирован |
| 12. Production hardening | GDPR export/deletion/retention, резервное копирование, observability, нагрузочная и security-проверка, E2E journey | Запланирован |

## Результат первого релиза

Первый релиз завершает этапы 1–2 и создаёт честную основу workspace: пользователь может создать аккаунт и организацию, войти, выйти, переключать доступные ему организации и видеть только доступные ему данные. Workspace показывает пустые состояния для ещё не реализованных возможностей. Агентские запуски, интеграции, документы и аналитика не будут изображаться работающими до реализации их backend-этапов.

## Проверки каждого этапа

Для каждого выпуска выполняются форматирование/lint, typecheck, unit и integration проверки по реализованной логике, сборка frontend/backend и проверка Docker image. Security-критерии включают отказ в межтенантном доступе, безопасные cookie/CSRF, ограничение попыток входа, проверку прав на сервере и отсутствие секретов в Git.
